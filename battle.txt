
battle:

run = false

hpEnemy = 100
hpPlayer = 100

userSpAttCount = 0
enemySpAttCount = 0


while ((!run) && (hpEnemy <= 0) && (hpPlayer <= 0)) do
	
	
	Tampilin stat musuh (nama enemy, level, attack, defense), dan aksi yang bisa dilkaukan  
	
	output(

	You found a goblin
	Level: 3
	Health: 100
	Attack: 54
	Defense: 30

	What will you do?

	|--------------|--------------|
	|    Attack    |Special Attack|
	|--------------|--------------|
	|    Potion    |     Run      |
	|--------------|--------------|

	)

	read(command)

	if (command = attack) then
		userAttacking
		enemyAttacking


	else if (command = Special Attack) then
		userSpecialAttack
		enemyAttacking


	 else if (command = potion) then
	 	if (potionCount > 0) then
	 		potionCount -= 1
	 		hpPlayer += potion
	 		enemyAttacking

	 	else
	 		read(command)
	 		while(command == potion)

	 		


	else if (command = Run) then
	 	run = getRunStatus() {akan true atau false jika berhasil}

	 	if (!run) then
	 		enemyAttacking



	userSpAttCount = 0
	enemySpAttCount = 0





